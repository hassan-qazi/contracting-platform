@model ContractingPlatform.Models.Contract

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Contract</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EntityA" class="control-label"></label><br/>
                <select asp-for="EntityA" class="control-label"  asp-items="ViewBag.EntityList">
                </select><br/>
                <span asp-validation-for="EntityA" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EntityB" class="control-label"></label><br/>
                <select asp-for="EntityB" class="control-label"  asp-items="ViewBag.EntityList">
                </select><br/>
                <span asp-validation-for="EntityB" class="text-danger"></span>
            </div>
            <div class="form-group">
                @{
                    if(ViewData["Error"] != null) {
                         <span class="text-danger">@ViewData["Error"] </span>
                    }
                }               
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
